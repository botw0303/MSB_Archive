using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;
using DG.Tweening;

public class PopUpText : MonoBehaviour
{
    //public virtual void SetUpText(string text, float fontSize, Vector2 offset)
    //{
    //    transform.position = offset;
    //    tmp.fontSize = fontSize;
    //    tmp.text = text;

    //    tmp.transform.DOMove(offset + new Vector2(0, 30), 2.5f);
    //}
}
